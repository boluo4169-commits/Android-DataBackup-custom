# 更新日志

本文件记录定制版相对原版 [XayahSuSuSu/Android-DataBackup](https://github.com/XayahSuSuSu/Android-DataBackup) 的改动。

## v3.6.7（2026-08-27）

### 新功能：数据迁移支持云端

- **导出到云端**：导出页新增「导出到云端」按钮，选择云端账号（WebDAV / FTP / SMB / SFTP）后，迁移包直接上传到云端 `migration/` 目录——换机/降级场景无需电脑中转，手机直连云备份
- **从云端导入**：导入页新增「从云端导入」，选云端账号 → 列出该云端 `migration/` 下的迁移包 → 下载导入；与本地导入完全一致（SHA-256 校验、危险条目整包拒绝、主备份 + 受保护版本原样保留）
- 云端迁移包的受保护版本处理与本地迁移包**完全一致**（共用同一套打包/解析逻辑，不混淆）

### 修复

- **导出报错（tar Cannot stat）**：导出列表曾混入「云端恢复」写入数据库的记录（本地没有对应目录），勾选导出必然失败；现导出页**只列本地备份记录**，并在打包前预检目录存在性，缺失时给出明确提示（替代模糊的 tar 报错）
- **云端迁移包文件名可读**：`DataBackup_migration_<epoch毫秒>.tar.zst` → `DataBackup_migration_<北京时间>.tar.zst`，云端目录里一眼分辨新旧（本地导出文件名同步）

### 配套工具（companion v1.2）

- **新增 `--diagnose` 诊断模式**：一键导出环境信息 / FTP 状态 / 备份文件清单 / 完整性检查（应用 config json 合法性、归档与 .md5 配对、0 字节文件、云端迁移包列表），生成 zip 到备份目录，配合手机端「导出日志」形成完整反馈材料
- 修复 bat 启动时联网检查版本导致**输入提示延迟 1~3 秒**（版本检查移至交互输入完成后）
- 修复 bat 中文编码兼容问题（cmd 对 UTF-8 批处理的 REM 中文注释解析异常，注释改为英文）
- 修复 exe banner 版本号双 v 显示（`vv3.6.6` → `v3.6.6`）

## v3.6.6（2026-08-25）

### 修复

- **备份大应用不再误报失败**：打包期间应用后台写入（典型：微信清缓存后系统/后台进程重建表情等目录）触发的 `file changed as we read it` 不再导致备份报错——GNU tar 该场景退出码 1 但归档完整，现自动保留并照常通过结构校验与 MD5；应用数据（USER/USER_DE/DATA/OBB/MEDIA）与媒体备份均已覆盖
- **删除双开空间应用的闪退**：恢复列表删除 user 999 等多用户空间下唯一的备份应用时，用户 Tab 索引未随列表缩短而校正，Material3 TabRow 测量越界闪退；已在数据层出口与两处 Tab UI 钳制索引
- **「立即检查并修复」功能失效**：属主扫描的 stat 格式串被按空格拆参，导致所有目录被误报为属主错位、修复全部失败；已修正引号传递
- **外部存储 SELinux 标签加固**：恢复 Android/{data,obb,media} 时不再信任恢复前残留标签（MediaProvider 可能提前创建出无 category 的错误标签目录），无条件走系统 `restorecon -R -F` 重打正确标签

### 改进（日志与诊断）

- **崩溃报告增强**：header 补充 versionCode/flavor/系统版本串/厂商，并自动附带崩溃前本进程最近 300 行 logcat
- **导出日志新增系统取证**：zip 内新增 system_evidence.txt（root 抓全系统 logcat 尾部 + 系统 dropbox 最近 crash/anr 条目），「恢复后目标应用闪退」类问题无需再抓 adb logcat
- **日志文件名改为日期时间格式**：`log_<毫秒时间戳>.txt` → `log_20260825_144401.txt`，一眼识别日志归属时段
- **R8 mapping 归档制度**：新增 gradle task 自动归档混淆映射表到本地 docs/mappings/，用户反馈的混淆崩溃堆栈可精确反解到源码行

## v3.6.5（2026-08-24）

### 新功能：修复数据属主

- **恢复设置新增「修复数据属主」开关**（默认开启）：每次恢复应用数据完成后，自动校验数据目录内所有文件的属主是否为当前应用 UID，发现卸载重装残留的旧属主文件自动修复，防止游戏因目录不可写而报错（如 UE4 更新失败 556793857）
- **设置页新增「数据修复」分区与手动触发**：可随时扫描所有应用的数据目录（内部 + 外部存储 Android/{data,obb,media}），一键修复属主错位；扫描支持深度检测（顶层目录属主正常但树内残留也能发现），会跳过其他活跃应用所属的目录（双开场景安全）

### 其他

- 桌面应用名称跟随系统语言：中文系统显示「数据备份」，英文及其他语言显示 DataBackup（此前正式版固定为英文名）
- **修复备份 APK 直接报错**：v3.6.0 安全加固时把 APK 备份命令的源路径参数误加引号，导致 `./*.apk` 通配符无法展开、tar 找不到文件而失败（数据备份不受影响）；恢复通配符正常展开，APK 备份已恢复

## v3.6.0（2026-08-23）

### 安全加固

- **修复 root shell 命令注入隐患**：此前 shell 命令里的路径参数直接拼接双引号，应用名等含特殊字符时可能被注入执行。现在统一走 POSIX 单引号转义（`SymbolUtil.shellQuote`），涉及备份、恢复、权限、SELinux、目录操作等所有 root 命令
- **迁移包导入加安全门闩**：包内条目名含危险字符（如 `../`、绝对路径）时整包拒绝导入，防止解压越权写入
- **迁移包支持完整性校验**：导出完成页展示 SHA-256 校验码（可一键复制）；导入页可粘贴校验码强校验，不匹配直接拒绝；本机导出历史自动记录，方便核对
- **WebDAV 强制 HTTPS**：未开启「允许不安全连接」时只允许 https 连接，账号密码不再明文传输
- **删除休眠的 dex 特权模块**：移除未启用的 dex/ 目录（84 个文件），减小仓库体积

### 其他

- 导出「加份保险」弹窗按钮调整：「知道了 / 继续导出」，点继续后直接开始导出

## v3.5.0（2026-08-19）

### 修复（澎湃 OS 专项）

- 修复澎湃 OS 设备数据迁移闪退与应用名乱码
- 修复大数据量迁移导出/导入时的 ANR（主线程阻塞）
- 修复备份详情页大小偶发闪现为 0

## v3.4.5（2026-08-19）

- 修复内容与 v3.5.0 相同（v3.5.0 为同修复集的重打包版本）

## v3.4.0（2026-08-19）

### 修复

- 修复跨系统大版本升级后（如澎湃 OS 3 → 移植的澎湃 OS 4）应用恢复失败的问题

### 新增

- 备份完整性检查：恢复前先验证备份包结构完整（`tar -tf` 结构测试）

## v3.3.1 / v3.3.0（2026-08-18）

- 与 v3.2.3 同一构建基线的重新打包发布，无独立改动

## v3.2.3（2026-08-18）

- 修复备份记录版本号：备份时从 PackageManager 读取真实安装版本，避免记录到缓存的旧版本号

## v3.2.2（2026-08-18）

### 新功能

- **数据迁移**：换机场景一键打包/导入
  - 导出：从已备份应用中勾选（支持搜索、用户空间筛选、展开勾选受保护版本），打包为迁移包（tar.zst）
  - 导入：选择迁移包自动解析包含的应用，一键解压到本机备份目录并刷新恢复列表
  - 迁移包含应用主备份 + 受保护版本，导入后原样保留

## v3.2.1（2026-08-17）

- 修复 WebDAV URL 编码问题
- 修复受保护版本时间显示
- 修复大小显示
- 「恢复权限」开关新增说明文案（澎湃系统用户建议关闭）

## v3.2.0（2026-08-17）

### 新功能

- 清除设备指纹：GAID / GSF ID 随机化

### 修复

- 修复权限恢复行为（拒绝的权限恢复后保持拒绝）

## v3.1.8（2026-08-15）

### 性能优化（修复澎湃 OS / 小米机型卡顿）

- 应用详情页数据大小计算改为并行 + 超时，不再卡在「计算中」
- 修复 native 遍历大目录时的空指针隐患
- 扫描本机应用时跳过不必要的存储统计，界面不再因扫描卡顿
- 移除列表条目位移动画，降低刷新卡顿

## v3.1.7（2026-08-15）

### 性能优化

- 应用图标加载增加全局内存缓存，图片转换移入后台线程，列表滑动更流畅

### 日志导出

- 设置 → 高级 → 导出日志，调起系统文件管理器自由选择保存位置
- 将近期日志打包为 zip 压缩包
- 应用崩溃堆栈自动写入日志，闪退后可导出分析

## v3.1.5（2026-08-15）

### 新功能

- **保留历史备份**：开关 + 最大保留版本数；最新备份为正常版本，历史自动标记为受保护（盾牌序号）
- **多版本标识**：恢复列表显示备份时间 + 护盾序号，一眼分辨新旧
- **多版本覆盖提醒**：同一应用多版本一起恢复时弹提醒
- **备份目录加应用名**：目录名改为「应用名_包名」，文件管理器里一眼识别

### 优化与修复

- 压缩等级优化：修复 zstd `--ultra` 冗余，选「TAR」时压缩等级滑杆置灰
- 「杀死应用选项」文案改为描述性
- 「随机化 Android ID」弹窗优化；「恢复 Android ID」不再弹窗
- 修复恢复列表多版本时间 / 护盾序号错乱
- 引导页「恢复设置」入口加图标
- 应用详情新增「文件路径」一键复制
- 备份目录名秒级时间戳，避免同分钟多次备份冲突

## v3.1.0（2026-08-14）

- 新增「随机化 Android ID」功能：恢复时可为应用生成全新的 Android ID
