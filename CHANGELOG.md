# 更新日志

本文件记录定制版相对原版 [XayahSuSuSu/Android-DataBackup](https://github.com/XayahSuSuSu/Android-DataBackup) 的改动。

## v3.13.1（2026-09-20）

### 修复

- **重载会把 `.md5` 校验文件当成归档遍历**：`walkFileTree` 返回的每个文件都会进一次分支判断，`xxx.tar.zst.md5` 的扩展名被解析成 `tar.zst.md5`、取不到压缩类型，于是每种类型白打一行 `Dumping xxx...` 和一行 `Failed to parse compression type`；分类阶段同样逐文件打印一行。现在统一在遍历前过滤掉 `.md5`。实测（9 个应用 / 13.96 GB）日志从 **445 行降到 274 行**，`Failed to parse` 由 **36 行归零**，`Total paths count` 也从 83（含水印文件）变成 46（只算真实归档）。
- **重载异常时界面永久停在加载中**：`isLoading = true` 之后没有兜底，执行体一旦抛出就回不到可用状态。现以 `try/finally` 收口，异常也会复位。
- **重载页「版本」显示硬编码英文 `current`**：中文界面里唯一的英文孤例。改为资源字符串 `reload_version_current`（当前版本 / 目前版本 / Current），随多语言一起生效。

### 优化

- **压缩等级 / 压缩线程数的说明补充**：两处各加一句"关闭只是折叠此项，仍按当前值执行，默认 1 / 默认 2"，避免被读成"关闭 = 不使用该设置"；同时去掉「压缩线程数」描述里的示例括号「（如 2）」。19 个语言同步。

## v3.13.0（2026-09-18）

### 新增

- **云端备份分卷上传**：备份设置 →「云端分卷大小」（关闭 / 1 GB / 2 GB / 4 GB，默认关闭）。云端上传的归档超过阈值时切成 `<归档>.part.aaa/.aab…` 逐卷上传，用于绕开部分网盘（WebDAV 等）的单文件大小限制；恢复/导入时自动按序下载并合并，`.md5` 仍是整档校验值。**本地备份不受影响，本地始终是一个完整文件**。覆盖三条云端上传路径：应用备份、文件备份、一键迁移导出。
- **归档过大提醒**：云端归档超过 10 GB 且未开启分卷时，在任务日志提示一次（同一次任务只提示一次），提示里带上实际大小。
- **应用内「更新日志」**：主页顶栏版本号徽章、设置 → 关于 的「更新日志」均可打开；展示当前版本与历史版本，数据来自仓库 `CHANGELOG.md`（随包离线可看，不再是跳浏览器）。

### 优化

- **澎湃系统默认关闭「恢复权限」**：HyperOS 上该开关是"恢复时强制授予全部权限"，与"还原备份时的授权状态"的预期不符，改为默认关闭、交给系统恢复；用户手动改过则以用户设置为准。
- **备份设置排版**：「云端分卷大小」移到「杀死应用选项」下方；「压缩等级」「压缩线程数」改为开关 + 可折叠（默认展开，行为与之前一致；默认值仍为等级 1、线程数 2）。

### 修复

- **恢复分卷备份时 user 数据被跳过**：恢复侧的存在性判断只看整档，而分卷上传后云端只有 `part.*`，会被当成"缺失"（实测报 `Failed to connect to cloud or file not exist`）；现统一为「整档存在 **或** 有分卷即为存在」。
- **分卷合并写出损坏文件**：合并原本走 `sh -c "cat … > …"`，App 的 shell 包装会在命令尾部追加哨兵串，被 `>` 重定向进目标文件（实测 1.73 GB 合并成 121 字节）；改为流式拼接 + 长度自校验。

## v3.12.1（2026-09-15）

### 修复

- **扫描应用列表时主线程被通知刷新占满**：每处理一个应用就更新一次前台通知，而系统更新通知只能在主线程执行，且每次含一次跨进程调用。实测（OPD2413 / Android 16，47 个第三方应用）扫描全程仅 145 ms，通知刷新却在 0.79 秒内触发 **291 次**、全部落在主线程，开销达实际工作量的 5 倍以上；应用数量上百时主线程被连续占满直至 ANR。现改为按 250 ms 合帧（首尾必发），复测 291 → 6 次。
- **后台启动时加载任务直接失败**：App 处于后台时系统拒绝 `startForegroundService()`（`ForegroundServiceStartNotAllowedException`，Android 12+ 限制），异常沿 `doWork` 冒泡使 Worker 以 failure 收场。实测后台冷启动下 `AppsLoadWorker` FAILED，恢复页备份列表加载不出来。现以 `setForegroundSafely` 统一吞掉该异常——通知只是进度展示，不应让业务失败（7 处调用点）。

### 优化

- **权限定义按权限名缓存**：`getPermissions` 原先对每条 requestedPermission 各调一次 `getPermissionInfo`，实测单应用 20 条权限，N 个应用即 N×M 次跨进程调用；权限定义是系统级共享的，改为进程级缓存（含 `permissionToOpCode` 映射）。
- **`SsaidUtil` 按 userId 复用**：其构造会新建 HandlerThread 并解析整个 ssaid 文件，原实现每次查询都 new 且从不 quit；改为按 userId 复用单例。
- **Room `Converters` 复用 Gson 实例**：原先每次调用都新建 `GsonUtil`（含 `GsonBuilder().create()`）并重建 TypeToken，而 converter 是逐行逐列调用的（实测 permissions 列平均 1385 B/行）。

### 工程

- 新增 `ForegroundProgressThrottler` 单元测试 7 条（首条 / 间隔内 / 末条 / 0-based 末尾 / 空区间 / 整场景收敛）；`core:util` 补上 `library.test` 插件。
- 复测方法沉淀：`build/analyze_notify.py`（通知次数 / 主线程归属 / 速率对比）、`build/perf_probe.sh`（设备侧原语耗时探针）。

## v3.12.0（2026-09-14）

### 新增

- **系统数据备份与恢复**：在原有「应用级备份」之外新增一整类系统数据备份，主页快捷操作新增「系统数据」入口，新增 `feature/main/system` 模块。
  - **短信**（含彩信）：`/data/data/com.android.providers.telephony/databases/mmssms.db`
  - **通话记录**：`/data/data/com.android.providers.contacts/databases/calllog.db`
  - **WiFi 网络**：`WifiConfigStore.xml`（Android 11+ 在 `apexdata` 下，旧版本回退 `/data/misc/wifi`）
- **本地 / 云端双通路**：三项均可选择存到本地（系统文件管理器选位置）或传到云端（`<远端目录>/system_data/`），云端三项各用真实文件名，避免互相覆盖。
- **恢复前的结构兼容性检查**：恢复数据库类数据前比对「本机现有库」与「备份库」的表 / 列结构，有差异时弹窗列出缺失项交用户决定。恢复是整库覆盖，跨品牌（一加的 `oplus_*` / `rcs_*` 对小米的 `miui_*`）覆盖后目标机 App 查询会落空，必须提前告知。

### 安全

- WiFi 备份文件中的密码为明文，界面明确提示；含凭据的命令行一律不写日志（实测导出日志 0 条残留）。
- 恢复时属主 / 权限 / SELinux 标签全部动态读取，不硬编码 uid（手机 `u0_a72`、平板 `u0_a73` 两种机型已验证）。

### 修复

- **结构检查漏判「整表缺失」**：早期实现只在「目标库的表在源库中存在」时才比对列，导致拿通话记录库恢复短信库时（`sms` / `threads` 等表整体不存在）被判定为「无差异」而静默覆盖。现整表缺失同样计入差异。

### 优化

- **「高级 - 历史记录」**：去掉前置图标，中文名称由「历史」改为「历史记录」，补齐副标题 —— 该行原本是区块内唯一没有副标题的项，行高与相邻项不一致。

### 工程

- 抽出 `SystemDataUtil.stageToCache` / `restoreFromStaged` 作为本地与云端两条通路的共用中间层，两条通路的差异仅剩「文件从哪来」。
- 云端下载不复用 `CloudRepository.download`：其内部先用 root 重建目标目录，属主变为 root 后应用写不进去（`EACCES`）；改为应用身份建目录（并显式 `chown` 兜底历史遗留的 root 属主）+ 直接调 `client.download`。
- 云端为网络操作，统一走 `withIOContext`，避免主线程 `NetworkOnMainThreadException`。

## v3.11.0（2026-09-13）

### 修复

- **云端导出误报失败**：上传成功但本地临时包清理失败时，导出会被判为失败——云端其实已有完整包，用户重试还会留下重复包。清理结果不再影响业务判定（清理失败仍写日志）。
- **WebDAV 上传缺完整性校验**：WebDAV 原本只判 HTTP 状态码，传输被截断也算成功，而本地唯一副本随后被删。现补写后远端大小比对（服务端不返回 `getcontentlength` 时跳过，避免误报）。
- **上传失败留远端残包**：FTP / SFTP / SMB / WebDAV 在传输失败或字节数不符时，清理远端半包。
- **导出页进页面 ANR**：备份目录扫描（十几 GB、海量小文件）从主线程移到 IO 线程。
- **转屏导致导出假失败**：导出改跑在 `viewModelScope`，屏幕旋转不再中断打包与上传。
- **旧版备份目录报「目录不存在」**：导出时探测「应用名_包名」与历史「纯包名」两种命名。

### 新增

- **云端导出专属成功文案**：显示「迁移包已上传到云端」，不再复用本地导出的「迁移包已写入所选位置」（18 种语言同步）。

### 优化

- **导出进度分 4 段**（校验 / 打包 / 校验码 / 上传），打包与哈希阶段给出真实字节数、速度与用时。
- **上传进度节流 300 ms**：WebDAV 每 256 KB 回调一次，十几 GB 包会产生数万次状态更新，进度条抖动。
- **防重复启动**：连点两次「导出到云端」只启动一次。

## v3.10.5（2026-09-12）

### 修复

- **云端备份崩溃**：开启「保留历史备份」时，若云端已有同名旧备份（典型：「云端目录仅用包名」开关在新旧版本之间翻过），再次备份会在归档旧目录时跨父目录重命名失败、应用直接崩溃。归档改为在源目录自身的父目录下进行——注意基类 `archiveMainBackup` 与云端 override 是两处，都要改。
- **云端备份删除无效**：删除路径没按「仅用包名」开关解析，开关开启时指向服务器上根本不存在的目录，结果是远端一个没删、本地记录却被误删，下次扫描又冒出来。现改为探测真实存在的目录；确实找不到时保留记录并写日志，不再出现「远端还在但本地记没了」的幽灵。
- **本地删除同类问题**：同样改为探测真实目录后再删。
- **详情页「文件路径」错位**：以前不管云端/本地、不管开关都按「应用名_包名」显示，与服务器上真实目录（开关开启时是纯包名）对不上。改为探测真实存在的目录后显示。
- **「保护」无法撤销**：「保护」原本是残缺的单向操作——一旦把备份标记为受保护（目录名加 `@时间戳`）就再也撤销不了，按钮直接置灰。现在可在「保护」/「取消保护」间正常切换；已存在普通版本时为避免覆盖会拒绝取消保护（不丢数据）。
- **删除后残留空文件夹**：删除备份后版本目录确实删掉了，但上一层的应用文件夹会变成空壳留在存储里。现在删除时即时清掉已空的应用目录，扫描时再兜底清一次历史残留（有其他版本在时不会被误删）。

### 工程

- 抽出 `PackageEntity.resolveArchivesRelativeDir(pkgOnly)` 与顶层 `preserveArchiveRelativeDir()` 统一目录名解析，补 13 个 JVM 单测（目录解析 7 + 体积统计 5 + 既有 1）。
- 全仓 `archivesRelativeDir` / `legacyArchivesRelativeDir` 用法普查：5 处行为类用法中 4 处曾有同类问题。
- 教训：改 `open`/`override` 方法必须 grep 全部覆盖点（本次就漏了云端 override，导致第一次没修好）。

## v3.10.1（2026-09-11）

### 修复

- **「应用」体积显示虚高**：只勾选「APK」时会把没勾选的「应用数据」「用户数据」等体积也算进去（实测 207 MB 显示成 552 MB），现在只统计实际勾选的内容。
- 恢复页同样修正：恢复列表和引导页不再把归档里并不存在的类型算进总大小。
- 补 5 个 JVM 单测锁住体积统计逻辑。

## v3.10.0（2026-09-11）

### 修复

- 返回列表页后顶部筛选按钮、切换用户空间失灵（两个列表页同时存在时状态断开）。
- 批量选择误勾系统应用：一键备份和「选择全部」现在只选用户自己装的应用，「加载系统应用」只负责是否显示；单独备份某个系统应用仍可手动勾选。
- 顶部已选 / 总数跟随「加载系统应用」开关，关掉后只统计第三方应用。
- 多用户空间删除后：恢复列表标注「用户（已删除）」，恢复目标只列真实存在的用户，备份页残留勾选自动清理。
- 没备份过的应用不再显示「文件路径」（那条路径实际并不存在）。

### 其他

- 补齐 15 种语言翻译；中文选项显示为「中文简体 / 中文繁体」，移除「中文（香港）」（原用户自动走繁体）。
- 一键备份引导页：勾选没变化时不重复计算大小；加载圈加粗并改为圆头。
- 备份设置页：杀死应用选项置顶、「保留历史备份」新装默认开启、「定时备份」入口移到「备份与迁移」页。

## v3.9.0（2026-09-06）

### ⚠️ 注意：签名更换

旧签名随目录误删丢失，改用新签名签发，**老用户必须卸载后重新安装**（可先用 App 内「备份自身配置」保存备份记录和云账号）。

### 新功能

- 「云端目录仅用包名」开关（默认开启）：解决 FTP / 网盘对中文目录名乱码、导致上传失败的问题。

### 修复

- 定时备份在冷启动时环境未初始化而失败（zstd not found / tar 参数错误）。
- 云端恢复列表在纯包名模式下识别不到备份的问题。
- 日志系统加固：崩溃信息更完整，导出日志自带系统崩溃档案。

## v3.6.7（2026-08-27）

### 新功能：数据迁移支持云端

- **导出到云端**：导出页新增「导出到云端」按钮，选择云端账号（WebDAV / FTP / SMB / SFTP）后，迁移包直接上传到云端 `migration/` 目录——换机/降级场景无需电脑中转，手机直连云备份
- **从云端导入**：导入页新增「从云端导入」，选云端账号 → 列出该云端 `migration/` 下的迁移包 → 下载导入；与本地导入完全一致（SHA-256 校验、危险条目整包拒绝、主备份 + 受保护版本原样保留）
- 云端迁移包的受保护版本处理与本地迁移包**完全一致**（共用同一套打包/解析逻辑，不混淆）

### 修复

- **导出报错（tar Cannot stat）**：导出列表曾混入「云端恢复」写入数据库的记录（本地没有对应目录），勾选导出必然失败；现导出页**只列本地备份记录**，并在打包前预检目录存在性，缺失时给出明确提示（替代模糊的 tar 报错）
- **云端迁移包文件名可读**：`DataBackup_migration_<epoch毫秒>.tar.zst` → `DataBackup_migration_<北京时间>.tar.zst`，云端目录里一眼分辨新旧（本地导出文件名同步）

### 配套工具（companion v1.2）

- **新增 `--diagnose` 诊断模式**：一键导出环境信息 / FTP 状态 / 备份文件清单 / 完整性检查（应用 config json 合法性、归档与 .md5 配对、0 字节文件、云端迁移包列表），生成 zip 到备份目录，配合手机端「导出日志」形成完整反馈材料
- 修复 bat 启动时联网检查版本导致**输入提示延迟 1~3 秒**（版本检查移至交互输入完成后）
- 修复 bat 中文编码兼容问题（cmd 对 UTF-8 批处理的 REM 中文注释解析异常，注释改为英文）
- 修复 exe banner 版本号双 v 显示（`vv3.6.6` → `v3.6.6`）

## v3.6.6（2026-08-25）

### 修复

- **备份大应用不再误报失败**：打包期间应用后台写入（典型：微信清缓存后系统/后台进程重建表情等目录）触发的 `file changed as we read it` 不再导致备份报错——GNU tar 该场景退出码 1 但归档完整，现自动保留并照常通过结构校验与 MD5；应用数据（USER/USER_DE/DATA/OBB/MEDIA）与媒体备份均已覆盖
- **删除双开空间应用的闪退**：恢复列表删除 user 999 等多用户空间下唯一的备份应用时，用户 Tab 索引未随列表缩短而校正，Material3 TabRow 测量越界闪退；已在数据层出口与两处 Tab UI 钳制索引
- **「立即检查并修复」功能失效**：属主扫描的 stat 格式串被按空格拆参，导致所有目录被误报为属主错位、修复全部失败；已修正引号传递
- **外部存储 SELinux 标签加固**：恢复 Android/{data,obb,media} 时不再信任恢复前残留标签（MediaProvider 可能提前创建出无 category 的错误标签目录），无条件走系统 `restorecon -R -F` 重打正确标签

### 改进（日志与诊断）

- **崩溃报告增强**：header 补充 versionCode/flavor/系统版本串/厂商，并自动附带崩溃前本进程最近 300 行 logcat
- **导出日志新增系统取证**：zip 内新增 system_evidence.txt（root 抓全系统 logcat 尾部 + 系统 dropbox 最近 crash/anr 条目），「恢复后目标应用闪退」类问题无需再抓 adb logcat
- **日志文件名改为日期时间格式**：`log_<毫秒时间戳>.txt` → `log_20260825_144401.txt`，一眼识别日志归属时段
- **R8 mapping 归档制度**：新增 gradle task 自动归档混淆映射表到本地 docs/mappings/，用户反馈的混淆崩溃堆栈可精确反解到源码行

## v3.6.5（2026-08-24）

### 新功能：修复数据属主

- **恢复设置新增「修复数据属主」开关**（默认开启）：每次恢复应用数据完成后，自动校验数据目录内所有文件的属主是否为当前应用 UID，发现卸载重装残留的旧属主文件自动修复，防止游戏因目录不可写而报错（如 UE4 更新失败 556793857）
- **设置页新增「数据修复」分区与手动触发**：可随时扫描所有应用的数据目录（内部 + 外部存储 Android/{data,obb,media}），一键修复属主错位；扫描支持深度检测（顶层目录属主正常但树内残留也能发现），会跳过其他活跃应用所属的目录（双开场景安全）

### 其他

- 桌面应用名称跟随系统语言：中文系统显示「数据备份」，英文及其他语言显示 DataBackup（此前正式版固定为英文名）
- **修复备份 APK 直接报错**：v3.6.0 安全加固时把 APK 备份命令的源路径参数误加引号，导致 `./*.apk` 通配符无法展开、tar 找不到文件而失败（数据备份不受影响）；恢复通配符正常展开，APK 备份已恢复

## v3.6.0（2026-08-23）

### 安全加固

- **修复 root shell 命令注入隐患**：此前 shell 命令里的路径参数直接拼接双引号，应用名等含特殊字符时可能被注入执行。现在统一走 POSIX 单引号转义（`SymbolUtil.shellQuote`），涉及备份、恢复、权限、SELinux、目录操作等所有 root 命令
- **迁移包导入加安全门闩**：包内条目名含危险字符（如 `../`、绝对路径）时整包拒绝导入，防止解压越权写入
- **迁移包支持完整性校验**：导出完成页展示 SHA-256 校验码（可一键复制）；导入页可粘贴校验码强校验，不匹配直接拒绝；本机导出历史自动记录，方便核对
- **WebDAV 强制 HTTPS**：未开启「允许不安全连接」时只允许 https 连接，账号密码不再明文传输
- **删除休眠的 dex 特权模块**：移除未启用的 dex/ 目录（84 个文件），减小仓库体积

### 其他

- 导出「加份保险」弹窗按钮调整：「知道了 / 继续导出」，点继续后直接开始导出

## v3.5.0（2026-08-19）

### 修复（澎湃 OS 专项）

- 修复澎湃 OS 设备数据迁移闪退与应用名乱码
- 修复大数据量迁移导出/导入时的 ANR（主线程阻塞）
- 修复备份详情页大小偶发闪现为 0

## v3.4.5（2026-08-19）

- 修复内容与 v3.5.0 相同（v3.5.0 为同修复集的重打包版本）

## v3.4.0（2026-08-19）

### 修复

- 修复跨系统大版本升级后（如澎湃 OS 3 → 移植的澎湃 OS 4）应用恢复失败的问题

### 新增

- 备份完整性检查：恢复前先验证备份包结构完整（`tar -tf` 结构测试）

## v3.3.1 / v3.3.0（2026-08-18）

- 与 v3.2.3 同一构建基线的重新打包发布，无独立改动

## v3.2.3（2026-08-18）

- 修复备份记录版本号：备份时从 PackageManager 读取真实安装版本，避免记录到缓存的旧版本号

## v3.2.2（2026-08-18）

### 新功能

- **数据迁移**：换机场景一键打包/导入
  - 导出：从已备份应用中勾选（支持搜索、用户空间筛选、展开勾选受保护版本），打包为迁移包（tar.zst）
  - 导入：选择迁移包自动解析包含的应用，一键解压到本机备份目录并刷新恢复列表
  - 迁移包含应用主备份 + 受保护版本，导入后原样保留

## v3.2.1（2026-08-17）

- 修复 WebDAV URL 编码问题
- 修复受保护版本时间显示
- 修复大小显示
- 「恢复权限」开关新增说明文案（澎湃系统用户建议关闭）

## v3.2.0（2026-08-17）

### 新功能

- 清除设备指纹：GAID / GSF ID 随机化

### 修复

- 修复权限恢复行为（拒绝的权限恢复后保持拒绝）

## v3.1.8（2026-08-15）

### 性能优化（修复澎湃 OS / 小米机型卡顿）

- 应用详情页数据大小计算改为并行 + 超时，不再卡在「计算中」
- 修复 native 遍历大目录时的空指针隐患
- 扫描本机应用时跳过不必要的存储统计，界面不再因扫描卡顿
- 移除列表条目位移动画，降低刷新卡顿

## v3.1.7（2026-08-15）

### 性能优化

- 应用图标加载增加全局内存缓存，图片转换移入后台线程，列表滑动更流畅

### 日志导出

- 设置 → 高级 → 导出日志，调起系统文件管理器自由选择保存位置
- 将近期日志打包为 zip 压缩包
- 应用崩溃堆栈自动写入日志，闪退后可导出分析

## v3.1.5（2026-08-15）

### 新功能

- **保留历史备份**：开关 + 最大保留版本数；最新备份为正常版本，历史自动标记为受保护（盾牌序号）
- **多版本标识**：恢复列表显示备份时间 + 护盾序号，一眼分辨新旧
- **多版本覆盖提醒**：同一应用多版本一起恢复时弹提醒
- **备份目录加应用名**：目录名改为「应用名_包名」，文件管理器里一眼识别

### 优化与修复

- 压缩等级优化：修复 zstd `--ultra` 冗余，选「TAR」时压缩等级滑杆置灰
- 「杀死应用选项」文案改为描述性
- 「随机化 Android ID」弹窗优化；「恢复 Android ID」不再弹窗
- 修复恢复列表多版本时间 / 护盾序号错乱
- 引导页「恢复设置」入口加图标
- 应用详情新增「文件路径」一键复制
- 备份目录名秒级时间戳，避免同分钟多次备份冲突

## v3.1.0（2026-08-14）

- 新增「随机化 Android ID」功能：恢复时可为应用生成全新的 Android ID
